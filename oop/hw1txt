В этом домашнем задании Вам нужно написать класс,
объектами которого будут строки в дореволюционной орфографии.
В этом классе должны быть реализованы функции сравнения,
которые позволят сортировать массивы с экземплярами этого класса в соответствии с порядком букв
в дореволюционном алфавите (который предлагается посмотреть в википедии). Тестировать я его буду примерно так:

s1 = OldString(u'бѣлый')  # или сначала s1 = OldString(), а потом, например, s1.text = u'бѣлый'
s2 = OldString(u'бебебе')
s3 = OldString(u'Ѳедоръ')
array = [s1, s2, s3]
for s in sorted(array):
    print s.text   # слова должны распечататься в алфавитном порядке

Если Вам не нравится дореволюционный алфавит,
можно взять любой другой алфавит, содержащий буквы с диакритиками,
который в питоне не сортируется автоматически так, как нужно,
только напишите в комментариях, что именно умеет сортировать Ваш класс.